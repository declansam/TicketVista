

{{#if error}}
    
    <h1 class="text-red-600 text-xl font-bold mb-4">{{error}}</h1>

{{/if}}

{{#if event}}

    <div class="edit-event-admin-container p-4 bg-white shadow-md rounded-md">

        {{!-- Title --}}
        <div class="title text-2xl font-bold mb-4">
            <h1>Edit the {{event.title}} event</h1>
        </div>

        <div class="edit-event-admin-form">
            
            <form action="/admin/events/edit/{{event._id}}" method="POST">

                <div class="mb-4">
                    <label for="title" class="block text-gray-600 text-sm font-semibold">Title:</label>
                    <input type="text" id="title" name="title" value="{{event.title}}" required class="w-full px-3 py-2 border rounded-md"><br>
                </div>

                <div class="mb-4">
                    <label for="date" class="block text-gray-600 text-sm font-semibold">Date:</label>
                    <input type="date" id="date" name="date" value="{{formattedDate}}" required class="w-full px-3 py-2 border rounded-md"><br>
                </div>

                <div class="mb-4">
                    <label for="venue" class="block text-gray-600 text-sm font-semibold">Venue:</label>
                    <input type="text" id="venue" name="venue" value="{{event.venue}}" required class="w-full px-3 py-2 border rounded-md"><br>
                </div>

                <div class="mb-4">
                    <label for="price" class="block text-gray-600 text-sm font-semibold">Price($):</label>
                    <input type="number" id="price" name="price" value="{{event.price}}" required class="w-full px-3 py-2 border rounded-md"><br>
                </div>
                
                <div class="mb-4">
                    <label for="description" class="block text-gray-600 text-sm font-semibold">Description:</label>
                    <textarea id="description" name="description" rows="5" required class="w-full px-3 py-2 border rounded-md">{{event.description}}</textarea><br>
                </div>
                
                <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md hover:shadow-md focus:outline-none focus:ring focus:ring-blue-300">
                    Save Changes
                </button>
            </form>

        </div>

    </div>


{{/if}}


